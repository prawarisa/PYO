export class User { // ประกาศคลาส User
  id?: string; // ตัวแปร id ที่เป็นแบบ Optional และประเภทข้อมูลเป็น string
  username?: string; // ตัวแปร username ที่เป็นแบบ Optional และประเภทข้อมูลเป็น string
  password?: string; // ตัวแปร password ที่เป็นแบบ Optional และประเภทข้อมูลเป็น string
  firstName?: string; // ตัวแปร firstName ที่เป็นแบบ Optional และประเภทข้อมูลเป็น string
  lastName?: string; // ตัวแปร lastName ที่เป็นแบบ Optional และประเภทข้อมูลเป็น string
  token?: string; // ตัวแปร token ที่เป็นแบบ Optional และประเภทข้อมูลเป็น string
}
